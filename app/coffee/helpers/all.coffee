define (require) ->
    Handlebars  = require 'Handlebars'
    ->

    ###############################
    ### Define all your custom helpers here
    ###############################

    # eg:
    # Handlebars.registerHelper 'match', (item1, item2, block) ->
    #     if item1 == item2
    #         return block